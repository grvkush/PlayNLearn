<Window x:Class="WPF_CRUD.PopUpMessage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_CRUD"
        mc:Ignorable="d"
        Title="Message"
        Height="300"
        Width="500"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"        
        >
    <Border x:Name="brdPopUp" BorderBrush="Black"
            BorderThickness="2"
            CornerRadius="20"
            Width="450"
            Height="270"
            Background="#FF1E1E1E"
            >
        
        <Grid >

            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Image x:Name="imgIcon" Grid.Row="0"
                   Width="130"
                   Height="130"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Margin="0,10,0,0"/>

            <TextBlock x:Name="txtMessage" Grid.Row="1"                       
                       FontSize="20"
                       Foreground="Black"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>

            <Button x:Name="btnOk" Grid.Row="2"
                    Style="{StaticResource RectangleButtonStyle}"
                    Content="OK"
                    FontSize="20"                    
                    FontWeight="Bold"
                    Foreground="Black"
                    Width="100"
                    Height="40"
                    Margin="0,10,0,20"
                    HorizontalAlignment="Center"
                    Click="btnOk_Click"/>
        </Grid>
        
    </Border>
</Window>
